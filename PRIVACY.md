# Privacy

The v0.1 server does not persist user submissions by default. Inputs are processed in memory to produce business context summaries, bottleneck hypotheses, risk snapshots, workflow recommendations, mini reports, and intake packets.

Configuration values are read from environment variables. Empty CTA URLs are not shown in generated output.
